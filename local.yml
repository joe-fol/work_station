---
- hosts: localhost
  become: true
  tasks:
  - name: Install htop
    apt: name=htop
  - name: Oh my zsh
    shell: sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"